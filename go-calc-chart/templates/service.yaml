apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-calc-chart.fullname" . }}
  labels:
    {{- include "go-calc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-calc-chart.selectorLabels" . | nindent 4 }}
